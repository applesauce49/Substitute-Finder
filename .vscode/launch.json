{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug React (Chrome)",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}/client/src"
        }
    ]
}